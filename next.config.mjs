/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
    domains: [], 
    loader: 'default', 
  },
  images: {
  qualities: [75, 100],
}
};


export default nextConfig;
